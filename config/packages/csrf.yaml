# Enable stateless CSRF protection for forms and logins/logouts
framework:
    form:
        csrf_protection: true  # ✅ Correct boolean value (not a string)
    csrf_protection:
        enabled: true  # ✅ Correct boolean value (not a string)
            - submit
            - authenticate
            - logout
